diff --git a/android/app/capacitor.build.gradle b/android/app/capacitor.build.gradle
index 9dcbd20..a497c10 100644
--- a/android/app/capacitor.build.gradle
+++ b/android/app/capacitor.build.gradle
@@ -17,7 +17,6 @@ dependencies {
     implementation project(':capacitor-push-notifications')
     implementation project(':capacitor-share')
     implementation project(':capacitor-splash-screen')
-    implementation project(':capacitor-status-bar')
 
 }
 
diff --git a/android/app/src/main/java/com/admin/mamam/MainActivity.java b/android/app/src/main/java/com/admin/mamam/MainActivity.java
index e18b7f7..fb87e1e 100644
--- a/android/app/src/main/java/com/admin/mamam/MainActivity.java
+++ b/android/app/src/main/java/com/admin/mamam/MainActivity.java
@@ -1,22 +1,5 @@
 package com.admin.mamam;
 
-import android.os.Bundle;
-import androidx.core.view.WindowCompat;
 import com.getcapacitor.BridgeActivity;
 
-public class MainActivity extends BridgeActivity {
-    @Override
-    public void onCreate(Bundle savedInstanceState) {
-        super.onCreate(savedInstanceState);
-        // Edge-to-edge modern (API 30+, tetap aman di API lama karena
-        // AndroidX WindowCompat sudah nge-handle fallback-nya sendiri).
-        // Ini WAJIB ada supaya WebView beneran dapet WindowInsets yang
-        // benar dari sistem — tanpa ini, env(safe-area-inset-top) &
-        // env(safe-area-inset-bottom) di CSS bisa balik 0px meskipun
-        // status bar/nav bar transparan, karena gak ada yang nyuplai
-        // ukuran insets-nya ke WebView. statusBarColor/navigationBarColor
-        // transparan di styles.xml cuma bikin dia transparan secara
-        // visual, bukan ngasih tau CSS seberapa besar area amannya.
-        WindowCompat.setDecorFitsSystemWindows(getWindow(), false);
-    }
-}
\ No newline at end of file
+public class MainActivity extends BridgeActivity {}
diff --git a/android/app/src/main/res/values/styles.xml b/android/app/src/main/res/values/styles.xml
index ccd12db..be874e5 100644
--- a/android/app/src/main/res/values/styles.xml
+++ b/android/app/src/main/res/values/styles.xml
@@ -1,5 +1,5 @@
 <?xml version="1.0" encoding="utf-8"?>
-<resources xmlns:tools="http://schemas.android.com/tools">
+<resources>
 
     <!-- Base application theme. -->
     <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
@@ -7,22 +7,6 @@
         <item name="colorPrimary">@color/colorPrimary</item>
         <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
         <item name="colorAccent">@color/colorAccent</item>
-        <!-- Edge-to-edge: konten digambar di belakang status bar & navigation
-             bar. windowTranslucentStatus (cara lama) SENGAJA tidak dipakai —
-             API itu deprecated & gak reliable lagi di targetSdk 35+ (Android
-             15 mewajibkan edge-to-edge), penyebab ikon jam/baterai jadi
-             belang/samar di percobaan sebelumnya. Setup edge-to-edge yang
-             beneran ngefek sekarang ada di MainActivity.java lewat
-             WindowCompat.setDecorFitsSystemWindows(false) (API modern).
-             statusBarColor tetap transparent sebagai base — warna SOLID yang
-             keliatan (mengikuti tema light/dark) diatur lewat Capacitor
-             StatusBar plugin di JS (App.jsx), jalan tiap app dibuka/tema
-             berubah. -->
-        <item name="android:statusBarColor">@android:color/transparent</item>
-        <item name="android:navigationBarColor">@android:color/transparent</item>
-        <!-- android:windowLayoutInDisplayCutoutMode butuh API 28+ tapi aman
-             dideklarasikan di sini (minSdk 24) — cuma no-op di API lama. -->
-        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="28">shortEdges</item>
     </style>
 
     <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
diff --git a/android/capacitor.settings.gradle b/android/capacitor.settings.gradle
index 8628ca7..5e7d885 100644
--- a/android/capacitor.settings.gradle
+++ b/android/capacitor.settings.gradle
@@ -25,6 +25,3 @@ project(':capacitor-share').projectDir = new File('../node_modules/@capacitor/sh
 
 include ':capacitor-splash-screen'
 project(':capacitor-splash-screen').projectDir = new File('../node_modules/@capacitor/splash-screen/android')
-
-include ':capacitor-status-bar'
-project(':capacitor-status-bar').projectDir = new File('../node_modules/@capacitor/status-bar/android')
diff --git a/capacitor.config.json b/capacitor.config.json
index 2f9144d..eda2e9f 100644
--- a/capacitor.config.json
+++ b/capacitor.config.json
@@ -7,11 +7,6 @@
     "cleartext": false
   },
   "plugins": {
-    "StatusBar": {
-      "overlaysWebView": true,
-      "style": "DARK",
-      "backgroundColor": "#00000000"
-    },
     "SplashScreen": {
       "launchShowDuration": 2000,
       "launchAutoHide": true,
diff --git a/fix-safe-area-edge-to-edge.patch b/fix-safe-area-edge-to-edge.patch
deleted file mode 100644
index 6cd3b1d..0000000
--- a/fix-safe-area-edge-to-edge.patch
+++ /dev/null
@@ -1,62 +0,0 @@
-diff --git a/android/app/src/main/java/com/admin/mamam/MainActivity.java b/android/app/src/main/java/com/admin/mamam/MainActivity.java
-index fa843ae..e18b7f7 100644
---- a/android/app/src/main/java/com/admin/mamam/MainActivity.java
-+++ b/android/app/src/main/java/com/admin/mamam/MainActivity.java
-@@ -1,5 +1,22 @@
- package com.admin.mamam;
- 
-+import android.os.Bundle;
-+import androidx.core.view.WindowCompat;
- import com.getcapacitor.BridgeActivity;
- 
--public class MainActivity extends BridgeActivity {}
-\ No newline at end of file
-+public class MainActivity extends BridgeActivity {
-+    @Override
-+    public void onCreate(Bundle savedInstanceState) {
-+        super.onCreate(savedInstanceState);
-+        // Edge-to-edge modern (API 30+, tetap aman di API lama karena
-+        // AndroidX WindowCompat sudah nge-handle fallback-nya sendiri).
-+        // Ini WAJIB ada supaya WebView beneran dapet WindowInsets yang
-+        // benar dari sistem — tanpa ini, env(safe-area-inset-top) &
-+        // env(safe-area-inset-bottom) di CSS bisa balik 0px meskipun
-+        // status bar/nav bar transparan, karena gak ada yang nyuplai
-+        // ukuran insets-nya ke WebView. statusBarColor/navigationBarColor
-+        // transparan di styles.xml cuma bikin dia transparan secara
-+        // visual, bukan ngasih tau CSS seberapa besar area amannya.
-+        WindowCompat.setDecorFitsSystemWindows(getWindow(), false);
-+    }
-+}
-\ No newline at end of file
-diff --git a/src/app/layout/BottomNav.jsx b/src/app/layout/BottomNav.jsx
-index 5648ea5..003417a 100644
---- a/src/app/layout/BottomNav.jsx
-+++ b/src/app/layout/BottomNav.jsx
-@@ -6,7 +6,10 @@ export default function BottomNav({
-     navigate,
- }) {
-     return (
--        <div className="short:hidden bg-white/95 dark:bg-slate-950/95 backdrop-blur-xl border-t border-slate-100/60 dark:border-slate-900 flex justify-around items-center h-16 shrink-0 z-30 print:hidden shadow-[0_-4px_20px_rgba(0,0,0,0.02)] dark:shadow-none">
-+        <div
-+            className="short:hidden bg-white/95 dark:bg-slate-950/95 backdrop-blur-xl border-t border-slate-100/60 dark:border-slate-900 flex justify-around items-center h-16 shrink-0 z-30 print:hidden shadow-[0_-4px_20px_rgba(0,0,0,0.02)] dark:shadow-none box-content"
-+            style={{ paddingBottom: 'env(safe-area-inset-bottom, 0px)' }}
-+        >
-             
-             {/* Tombol Beranda */}
-             <button
-diff --git a/src/app/layout/Header.jsx b/src/app/layout/Header.jsx
-index 99304fc..8e85514 100644
---- a/src/app/layout/Header.jsx
-+++ b/src/app/layout/Header.jsx
-@@ -15,7 +15,10 @@ export default function Header({
-     salesHistory,
- }) {
-     return (
--        <header className="bg-white/95 dark:bg-slate-950/95 backdrop-blur-xl border-b border-slate-100/60 dark:border-slate-900 h-16 short:h-12 flex items-center justify-between px-4 short:px-3 z-20 shadow-[0_4px_20px_rgba(0,0,0,0.02)] dark:shadow-none shrink-0">
-+        <header
-+            className="bg-white/95 dark:bg-slate-950/95 backdrop-blur-xl border-b border-slate-100/60 dark:border-slate-900 h-16 short:h-12 flex items-center justify-between px-4 short:px-3 z-20 shadow-[0_4px_20px_rgba(0,0,0,0.02)] dark:shadow-none shrink-0 box-content"
-+            style={{ paddingTop: 'env(safe-area-inset-top, 0px)' }}
-+        >
-             <div className="flex items-center gap-3 short:gap-2">
-                 <button
-                     className="p-2.5 short:p-2 hover:bg-slate-100 dark:hover:bg-slate-900 active:scale-95 rounded-xl md:hidden short:!block text-slate-500 dark:text-slate-400 hover:text-slate-900 dark:hover:text-slate-100 transition-all duration-300"
diff --git a/fullscreen-fix.patch b/fullscreen-fix.patch
deleted file mode 100644
index 61ed98e..0000000
--- a/fullscreen-fix.patch
+++ /dev/null
@@ -1,141 +0,0 @@
-diff --git a/android/app/capacitor.build.gradle b/android/app/capacitor.build.gradle
-index a497c10..9dcbd20 100644
---- a/android/app/capacitor.build.gradle
-+++ b/android/app/capacitor.build.gradle
-@@ -17,6 +17,7 @@ dependencies {
-     implementation project(':capacitor-push-notifications')
-     implementation project(':capacitor-share')
-     implementation project(':capacitor-splash-screen')
-+    implementation project(':capacitor-status-bar')
- 
- }
- 
-diff --git a/android/app/src/main/res/values/styles.xml b/android/app/src/main/res/values/styles.xml
-index be874e5..497cc47 100644
---- a/android/app/src/main/res/values/styles.xml
-+++ b/android/app/src/main/res/values/styles.xml
-@@ -1,5 +1,5 @@
- <?xml version="1.0" encoding="utf-8"?>
--<resources>
-+<resources xmlns:tools="http://schemas.android.com/tools">
- 
-     <!-- Base application theme. -->
-     <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
-@@ -7,6 +7,16 @@
-         <item name="colorPrimary">@color/colorPrimary</item>
-         <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
-         <item name="colorAccent">@color/colorAccent</item>
-+        <!-- Edge-to-edge: biarin konten digambar di belakang status bar &
-+             navigation bar. Ini yang bikin StatusBar.setOverlaysWebView(true)
-+             dari Capacitor plugin beneran ngefek — kalau cuma diset di JS
-+             tanpa ini, webview tetep "didorong turun" oleh sistem window
-+             insets bawaan Android, jadi kelihatan sama aja kayak sebelumnya. -->
-+        <item name="android:windowTranslucentStatus">true</item>
-+        <item name="android:statusBarColor">@android:color/transparent</item>
-+        <!-- android:windowLayoutInDisplayCutoutMode butuh API 28+ tapi aman
-+             dideklarasikan di sini (minSdk 24) — cuma no-op di API lama. -->
-+        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="28">shortEdges</item>
-     </style>
- 
-     <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
-diff --git a/android/capacitor.settings.gradle b/android/capacitor.settings.gradle
-index 5e7d885..8628ca7 100644
---- a/android/capacitor.settings.gradle
-+++ b/android/capacitor.settings.gradle
-@@ -25,3 +25,6 @@ project(':capacitor-share').projectDir = new File('../node_modules/@capacitor/sh
- 
- include ':capacitor-splash-screen'
- project(':capacitor-splash-screen').projectDir = new File('../node_modules/@capacitor/splash-screen/android')
-+
-+include ':capacitor-status-bar'
-+project(':capacitor-status-bar').projectDir = new File('../node_modules/@capacitor/status-bar/android')
-diff --git a/capacitor.config.json b/capacitor.config.json
-index eda2e9f..2f9144d 100644
---- a/capacitor.config.json
-+++ b/capacitor.config.json
-@@ -7,6 +7,11 @@
-     "cleartext": false
-   },
-   "plugins": {
-+    "StatusBar": {
-+      "overlaysWebView": true,
-+      "style": "DARK",
-+      "backgroundColor": "#00000000"
-+    },
-     "SplashScreen": {
-       "launchShowDuration": 2000,
-       "launchAutoHide": true,
-diff --git a/index.html b/index.html
-index d5b2606..c91bc11 100644
---- a/index.html
-+++ b/index.html
-@@ -8,7 +8,7 @@
-     rel="stylesheet">
-   <meta charset="UTF-8" />
-   <link rel="icon" type="image/png" href="/favicon.png" />
--  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
-+  <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
-   <title>Mamam Global</title>
- </head>
- 
-diff --git a/src/app/App.jsx b/src/app/App.jsx
-index a26cb6d..3976165 100644
---- a/src/app/App.jsx
-+++ b/src/app/App.jsx
-@@ -1,5 +1,7 @@
- import React, { useState, useMemo, useEffect, useRef, useCallback } from 'react';
- import { App as CapacitorApp } from '@capacitor/app';
-+import { StatusBar, Style } from '@capacitor/status-bar';
-+import { Capacitor } from '@capacitor/core';
- import { usePersistState } from '../hook/usePersistState';
- import { useOnlineStatus } from '../hook/useOnlineStatus';
- import { INITIAL_MENUS, INITIAL_VARIANT_GROUPS, INITIAL_CATEGORIES, INITIAL_RAW_MATERIALS } from '../data/initialData';
-@@ -215,6 +217,31 @@ export default function App() {
-   const [theme, setTheme, l23] = usePersistState('theme', 'light');
-   const [colorTheme, setColorThemeState] = usePersistState('colorTheme', 'orange');
- 
-+  // ─── Status bar edge-to-edge ────────────────────────────────────────────
-+  // overlaysWebView(true): webview digambar di BELAKANG status bar (bukan
-+  // di bawahnya) — ini yang bikin app kerasa "fullscreen", status bar jadi
-+  // transparan menyatu sama warna header, tapi jam/baterai tetap keliatan.
-+  // Konten aktual (Header, Sidebar) tetap aman dari ketiban status bar
-+  // karena AppLayout kasih padding-top sebesar safe-area-inset-top (lihat
-+  // env(safe-area-inset-top) di AppLayout.jsx).
-+  // Cuma jalan di native (Android/iOS); di browser biasa plugin ini no-op
-+  // tapi dibungkus try/catch + cek platform biar aman.
-+  // Ikut `theme` supaya warna ikon status bar (jam/baterai) selalu kebaca
-+  // jelas: Style.Dark (ikon gelap) di atas header terang, Style.Light
-+  // (ikon terang) di atas header gelap saat dark mode aktif.
-+  useEffect(() => {
-+    if (!Capacitor.isNativePlatform()) return;
-+    (async () => {
-+      try {
-+        await StatusBar.setOverlaysWebView({ overlay: true });
-+        await StatusBar.setStyle({ style: theme === 'dark' ? Style.Light : Style.Dark });
-+        await StatusBar.setBackgroundColor({ color: '#00000000' });
-+      } catch (err) {
-+        console.warn('StatusBar setup gagal (mungkin platform tidak didukung):', err);
-+      }
-+    })();
-+  }, [theme]);
-+
-   useEffect(() => {
-     document.documentElement.setAttribute('data-color-theme', colorTheme);
-   }, [colorTheme]);
-diff --git a/src/app/AppLayout.jsx b/src/app/AppLayout.jsx
-index a327c48..c86114e 100644
---- a/src/app/AppLayout.jsx
-+++ b/src/app/AppLayout.jsx
-@@ -110,6 +110,12 @@ export default function AppLayout({
-     <div
-       ref={containerRef}
-       className="flex h-screen bg-slate-50 dark:bg-slate-950 font-body text-slate-800 dark:text-slate-100 overflow-hidden w-full relative"
-+      // paddingTop = tinggi status bar (notch/punch-hole android, dynamic
-+      // island/notch iOS). Diberikan di container terluar (bukan di Header
-+      // saja) supaya Sidebar overlay ikut turun dan tidak ketiban status
-+      // bar juga. env(safe-area-inset-top) bernilai 0 di browser biasa,
-+      // jadi aman dipakai terus tanpa perlu cek platform.
-+      style={{ paddingTop: 'env(safe-area-inset-top)' }}
-     >
-       {sidebar}
- 
diff --git a/index.html b/index.html
index c91bc11..d5b2606 100644
--- a/index.html
+++ b/index.html
@@ -8,7 +8,7 @@
     rel="stylesheet">
   <meta charset="UTF-8" />
   <link rel="icon" type="image/png" href="/favicon.png" />
-  <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
+  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
   <title>Mamam Global</title>
 </head>
 
diff --git a/package-lock.json b/package-lock.json
index 5b0e592..b9a5fb9 100644
--- a/package-lock.json
+++ b/package-lock.json
@@ -18,7 +18,6 @@
         "@capacitor/push-notifications": "^8.1.2",
         "@capacitor/share": "^8.0.1",
         "@capacitor/splash-screen": "^8.0.1",
-        "@capacitor/status-bar": "^8.0.3",
         "@dnd-kit/core": "^6.3.1",
         "@dnd-kit/sortable": "^10.0.0",
         "@dnd-kit/utilities": "^3.2.2",
@@ -2174,15 +2173,6 @@
         "@capacitor/core": ">=8.0.0"
       }
     },
-    "node_modules/@capacitor/status-bar": {
-      "version": "8.0.3",
-      "resolved": "https://registry.npmjs.org/@capacitor/status-bar/-/status-bar-8.0.3.tgz",
-      "integrity": "sha512-csSpfNeN49Hx9JaQBSJEIiEbOLtXg3kcc2IpScq2fu5L520h3AWEvsxoH8Srk1jxfRKepaJ4S4sqSC3foI4AgA==",
-      "license": "MIT",
-      "peerDependencies": {
-        "@capacitor/core": ">=8.0.0"
-      }
-    },
     "node_modules/@capacitor/synapse": {
       "version": "1.0.4",
       "resolved": "https://registry.npmjs.org/@capacitor/synapse/-/synapse-1.0.4.tgz",
diff --git a/package.json b/package.json
index 3d5316e..75cf045 100644
--- a/package.json
+++ b/package.json
@@ -20,7 +20,6 @@
     "@capacitor/push-notifications": "^8.1.2",
     "@capacitor/share": "^8.0.1",
     "@capacitor/splash-screen": "^8.0.1",
-    "@capacitor/status-bar": "^8.0.3",
     "@dnd-kit/core": "^6.3.1",
     "@dnd-kit/sortable": "^10.0.0",
     "@dnd-kit/utilities": "^3.2.2",
diff --git a/src/app/App.jsx b/src/app/App.jsx
index a9dd193..32aab7f 100644
--- a/src/app/App.jsx
+++ b/src/app/App.jsx
@@ -1,7 +1,5 @@
 import React, { useState, useMemo, useEffect, useRef, useCallback } from 'react';
 import { App as CapacitorApp } from '@capacitor/app';
-import { StatusBar, Style } from '@capacitor/status-bar';
-import { Capacitor } from '@capacitor/core';
 import { usePersistState } from '../hook/usePersistState';
 import { useOnlineStatus } from '../hook/useOnlineStatus';
 import { INITIAL_MENUS, INITIAL_VARIANT_GROUPS, INITIAL_CATEGORIES, INITIAL_RAW_MATERIALS } from '../data/initialData';
@@ -217,36 +215,6 @@ export default function App() {
   const [theme, setTheme, l23] = usePersistState('theme', 'light');
   const [colorTheme, setColorThemeState] = usePersistState('colorTheme', 'orange');
 
-  // ─── Status bar transparan edge-to-edge ─────────────────────────────────
-  // overlaysWebView(true): webview digambar DI BELAKANG status bar (bukan
-  // didorong ke bawahnya) — status bar jadi transparan, otomatis nyatu
-  // dengan apa pun yang ada di baliknya (Header bg-white/95 light,
-  // bg-slate-950/95 dark). Ini beda dari percobaan lama yang gagal: dulu
-  // edge-to-edge diaktifkan lewat windowTranslucentStatus di styles.xml
-  // (API lama, deprecated & gak reliable di targetSdk 35+/Android 15),
-  // yang bikin ikon jam/baterai jadi belang/samar. Sekarang edge-to-edge
-  // beneran diaktifkan lewat WindowCompat.setDecorFitsSystemWindows(false)
-  // di MainActivity.java (API modern) — jauh lebih stabil.
-  // Konten aman dari ketiban status bar karena AppLayout & Sidebar kasih
-  // padding-top sebesar env(safe-area-inset-top) (lihat AppLayout.jsx).
-  // Style ikon tetap ikut `theme`: Style.Dark (ikon gelap) di atas Header
-  // terang (light mode), Style.Light (ikon terang) di atas Header gelap
-  // (dark mode) — supaya jam/baterai/notifikasi selalu kontras & kebaca.
-  // Cuma jalan di native (Android/iOS); di browser biasa plugin ini no-op
-  // tapi dibungkus try/catch + cek platform biar aman.
-  useEffect(() => {
-    if (!Capacitor.isNativePlatform()) return;
-    (async () => {
-      try {
-        await StatusBar.setOverlaysWebView({ overlay: true });
-        await StatusBar.setBackgroundColor({ color: '#00000000' });
-        await StatusBar.setStyle({ style: theme === 'dark' ? Style.Light : Style.Dark });
-      } catch (err) {
-        console.warn('StatusBar setup gagal (mungkin platform tidak didukung):', err);
-      }
-    })();
-  }, [theme]);
-
   useEffect(() => {
     document.documentElement.setAttribute('data-color-theme', colorTheme);
   }, [colorTheme]);
diff --git a/src/app/layout/BottomNav.jsx b/src/app/layout/BottomNav.jsx
index 003417a..5648ea5 100644
--- a/src/app/layout/BottomNav.jsx
+++ b/src/app/layout/BottomNav.jsx
@@ -6,10 +6,7 @@ export default function BottomNav({
     navigate,
 }) {
     return (
-        <div
-            className="short:hidden bg-white/95 dark:bg-slate-950/95 backdrop-blur-xl border-t border-slate-100/60 dark:border-slate-900 flex justify-around items-center h-16 shrink-0 z-30 print:hidden shadow-[0_-4px_20px_rgba(0,0,0,0.02)] dark:shadow-none box-content"
-            style={{ paddingBottom: 'env(safe-area-inset-bottom, 0px)' }}
-        >
+        <div className="short:hidden bg-white/95 dark:bg-slate-950/95 backdrop-blur-xl border-t border-slate-100/60 dark:border-slate-900 flex justify-around items-center h-16 shrink-0 z-30 print:hidden shadow-[0_-4px_20px_rgba(0,0,0,0.02)] dark:shadow-none">
             
             {/* Tombol Beranda */}
             <button
diff --git a/src/app/layout/Header.jsx b/src/app/layout/Header.jsx
index 8e85514..99304fc 100644
--- a/src/app/layout/Header.jsx
+++ b/src/app/layout/Header.jsx
@@ -15,10 +15,7 @@ export default function Header({
     salesHistory,
 }) {
     return (
-        <header
-            className="bg-white/95 dark:bg-slate-950/95 backdrop-blur-xl border-b border-slate-100/60 dark:border-slate-900 h-16 short:h-12 flex items-center justify-between px-4 short:px-3 z-20 shadow-[0_4px_20px_rgba(0,0,0,0.02)] dark:shadow-none shrink-0 box-content"
-            style={{ paddingTop: 'env(safe-area-inset-top, 0px)' }}
-        >
+        <header className="bg-white/95 dark:bg-slate-950/95 backdrop-blur-xl border-b border-slate-100/60 dark:border-slate-900 h-16 short:h-12 flex items-center justify-between px-4 short:px-3 z-20 shadow-[0_4px_20px_rgba(0,0,0,0.02)] dark:shadow-none shrink-0">
             <div className="flex items-center gap-3 short:gap-2">
                 <button
                     className="p-2.5 short:p-2 hover:bg-slate-100 dark:hover:bg-slate-900 active:scale-95 rounded-xl md:hidden short:!block text-slate-500 dark:text-slate-400 hover:text-slate-900 dark:hover:text-slate-100 transition-all duration-300"
diff --git a/src/app/layout/Sidebar.jsx b/src/app/layout/Sidebar.jsx
index a344243..7847719 100644
--- a/src/app/layout/Sidebar.jsx
+++ b/src/app/layout/Sidebar.jsx
@@ -24,10 +24,7 @@ export default function Sidebar({
         <aside className={`fixed md:static short:!fixed inset-y-0 left-0 z-50 w-[268px] short:w-[240px] bg-white/95 dark:bg-slate-950/95 backdrop-blur-xl md:bg-white md:dark:bg-slate-950 short:!bg-white/95 short:dark:!bg-slate-950/95 short:!backdrop-blur-xl border-r border-slate-100/60 dark:border-slate-900 transition-transform duration-400 ease-out flex flex-col shadow-[4px_0_24px_rgba(0,0,0,0.02)] dark:shadow-none ${isSidebarOpen ? 'translate-x-0' : '-translate-x-full md:translate-x-0 short:!-translate-x-full'}`}>
 
             {/* Header: Logo mark + Gradient Text */}
-            <div
-                className="pb-6 px-6 flex items-start justify-between shrink-0"
-                style={{ paddingTop: 'calc(env(safe-area-inset-top, 0px) + 1.75rem)' }}
-            >
+            <div className="pt-7 pb-6 px-6 flex items-start justify-between shrink-0">
                 <div className="flex items-center gap-3">
                     <div className="relative shrink-0 w-11 h-11 rounded-2xl bg-gradient-to-br from-accent-500 to-accent-600 dark:from-accent-400 dark:to-accent-600 flex items-center justify-center shadow-[0_4px_14px_rgba(var(--color-accent-500),0.35)]">
                         <span className="font-heading font-black text-lg text-white">{initial}</span>
diff --git a/src/components/ui/Modal.jsx b/src/components/ui/Modal.jsx
index 6f6f810..0746bac 100644
--- a/src/components/ui/Modal.jsx
+++ b/src/components/ui/Modal.jsx
@@ -110,11 +110,9 @@ export default function Modal({
             ${className}
           `}
         >
-          {/* Header opsional — safe-top wajib di sini karena panel ini
-              full-height (h-full) dan nempel persis ke tepi atas layar,
-              sama kayak CartDrawer sebelum di-fix. */}
+          {/* Header opsional */}
           {title && (
-            <div className="safe-top flex items-center justify-between gap-3 p-5 pb-3 shrink-0 border-b border-slate-100 dark:border-slate-800">
+            <div className="flex items-center justify-between gap-3 p-5 pb-3 shrink-0 border-b border-slate-100 dark:border-slate-800">
               <h3 className="font-heading font-bold text-slate-900 dark:text-slate-50 text-lg min-w-0 truncate">{title}</h3>
               {onClose && (
                 <button
@@ -127,11 +125,8 @@ export default function Modal({
             </div>
           )}
 
-          {/* Konten — selalu scrollable karena panel udah full-height.
-              safe-bottom di sini (bukan di footer statis) karena variant
-              ini gak punya footer baku — kalau caller taruh tombol aksi
-              di ujung children, dia ikut ke-push oleh padding ini. */}
-          <div className="overflow-y-auto flex-1 safe-bottom">
+          {/* Konten — selalu scrollable karena panel udah full-height */}
+          <div className="overflow-y-auto flex-1">
             {children}
           </div>
         </div>
diff --git a/src/features/pos/CartDrawer.jsx b/src/features/pos/CartDrawer.jsx
index 46214f3..815f6c7 100644
--- a/src/features/pos/CartDrawer.jsx
+++ b/src/features/pos/CartDrawer.jsx
@@ -141,7 +141,7 @@ const CartDrawer = () => {
       <div className="absolute inset-0 bg-slate-500/40 dark:bg-slate-800/40 backdrop-blur-sm transition-opacity duration-300" onClick={() => setIsCartOpen(false)} />
       <div className="w-full md:w-[420px] bg-white dark:bg-slate-900 h-full flex flex-col shadow-2xl relative animate-in slide-in-from-right duration-300 ease-out">
 
-        <div className="safe-top p-4 border-b border-slate-100 dark:border-slate-800 flex justify-between items-center bg-white dark:bg-slate-900">
+        <div className="p-4 border-b border-slate-100 dark:border-slate-800 flex justify-between items-center bg-white dark:bg-slate-900">
           <h2 className="font-heading text-lg font-bold text-slate-800 dark:text-slate-100 flex items-center gap-2">
             <ShoppingCart className="w-5 h-5 text-slate-800 dark:text-slate-100" /> Keranjang Pesanan
           </h2>
@@ -400,7 +400,7 @@ const CartDrawer = () => {
 
         {/* --- CHECKOUT SUMMARY --- */}
         {cart.length > 0 && (
-          <div className="safe-bottom p-4 bg-white dark:bg-slate-900 border-t border-slate-100 dark:border-slate-800 shadow-[0_-4px_10px_rgba(0,0,0,0.03)] animate-in slide-in-from-bottom-2 duration-300">
+          <div className="p-4 bg-white dark:bg-slate-900 border-t border-slate-100 dark:border-slate-800 shadow-[0_-4px_10px_rgba(0,0,0,0.03)] animate-in slide-in-from-bottom-2 duration-300">
             <div className="space-y-1.5 mb-4 text-sm">
               <div className="flex justify-between text-slate-500 dark:text-slate-400"><span>Subtotal</span><span className="font-semibold">{formatRupiah(getSubtotal())}</span></div>
 
diff --git a/src/features/pos/PosView.jsx b/src/features/pos/PosView.jsx
index 347525d..f530fe2 100644
--- a/src/features/pos/PosView.jsx
+++ b/src/features/pos/PosView.jsx
@@ -427,10 +427,7 @@ const PosView = () => {
             </div>
 
             {/* ── FAB keranjang ───────────────────────────────────────────── */}
-            <div
-                className="fixed right-6 short:!right-4 z-50 bottom-[var(--fab-bottom)] short:!bottom-4"
-                style={{ '--fab-bottom': 'calc(4rem + env(safe-area-inset-bottom, 0px) + 1rem)' }}
-            >
+            <div className="fixed bottom-24 short:!bottom-4 right-6 short:!right-4 z-50">
                 <button
                     onClick={() => setIsCartOpen(true)}
                     className="bg-gradient-to-r from-accent-600 to-accent-500 dark:from-accent-500 dark:to-accent-600 text-white rounded-full py-3 short:!py-2 px-5 short:!px-4 shadow-[0_10px_28px_rgba(var(--color-accent-500),0.4)] flex items-center gap-3 short:!gap-2 hover:-translate-y-0.5 hover:shadow-[0_14px_32px_rgba(var(--color-accent-500),0.45)] transition-all duration-300 active:scale-95"
@@ -583,7 +580,7 @@ const PosView = () => {
                         </div>
 
                         {/* Footer: Reset + Terapkan */}
-                        <div className="safe-bottom flex items-center gap-3 px-5 py-4 border-t border-slate-100 dark:border-slate-800 shrink-0">
+                        <div className="flex items-center gap-3 px-5 py-4 border-t border-slate-100 dark:border-slate-800 shrink-0">
                             <button
                                 onClick={handleResetFilter}
                                 className="flex-1 py-3 rounded-full border border-slate-200 dark:border-slate-700 font-bold text-sm text-slate-700 dark:text-slate-200 active:scale-95 transition-transform duration-200"
diff --git a/src/features/pos/VariantSelectionModal.jsx b/src/features/pos/VariantSelectionModal.jsx
index 3dfceb4..37b535a 100644
--- a/src/features/pos/VariantSelectionModal.jsx
+++ b/src/features/pos/VariantSelectionModal.jsx
@@ -148,7 +148,7 @@ const VariantSelectionModal = () => {
           })}
         </div>
 
-        <div className="safe-bottom p-5 border-t border-slate-100 dark:border-slate-800 bg-white dark:bg-slate-900 shadow-[0_-10px_20px_rgba(0,0,0,0.02)]">
+        <div className="p-5 border-t border-slate-100 dark:border-slate-800 bg-white dark:bg-slate-900 shadow-[0_-10px_20px_rgba(0,0,0,0.02)]">
           <button
             onClick={handleSave}
             disabled={!isSelectionValid}
diff --git a/src/index.css b/src/index.css
index 6686930..fb9d88f 100644
--- a/src/index.css
+++ b/src/index.css
@@ -119,41 +119,3 @@
 .scrollbar-hide::-webkit-scrollbar {
   display: none;
 }
-
-/* ======================================================
-   SAFE AREA (edge-to-edge / overlaysWebView Capacitor)
-   ======================================================
-   StatusBar.overlaysWebView = true bikin WebView gambar di
-   BELAKANG status bar & nav bar (biar look-nya native/full
-   screen). Konsekuensinya: TIAP header/footer yang nempel
-   ke tepi layar (modal, drawer, sticky header, bottom nav,
-   bottom sheet) WAJIB pakai padding sebesar safe-area-inset,
-   kalau enggak dia bakal ketiban jam/status bar atau nabrak
-   gesture bar Android — persis kejadian di CartDrawer &
-   VariantSelectionModal sebelum fix ini.
-
-   env(safe-area-inset-*) cuma punya nilai kalau meta viewport
-   ada viewport-fit=cover (sudah ada di index.html). Fallback
-   0px dikasih biar aman kalau dibuka di browser desktop biasa.
-   ====================================================== */
-.safe-top {
-  padding-top: env(safe-area-inset-top, 0px);
-}
-.safe-bottom {
-  padding-bottom: env(safe-area-inset-bottom, 0px);
-}
-.safe-x {
-  padding-left: env(safe-area-inset-left, 0px);
-  padding-right: env(safe-area-inset-right, 0px);
-}
-
-/* Varian "tambah" — dipakai di elemen yang sudah punya padding
-   sendiri (mis. p-4/p-5) dan cuma perlu NAMBAH sisa inset di
-   atasnya, bukan menimpanya. Pakai calc() supaya padding asli
-   dari Tailwind (p-4 dsb) tetap kepakai. */
-.safe-top-add {
-  padding-top: calc(env(safe-area-inset-top, 0px) + var(--safe-add, 1rem));
-}
-.safe-bottom-add {
-  padding-bottom: calc(env(safe-area-inset-bottom, 0px) + var(--safe-add, 1rem));
-}
\ No newline at end of file
diff --git a/statusbar-transparent-fix.patch b/statusbar-transparent-fix.patch
deleted file mode 100644
index 609f6e2..0000000
--- a/statusbar-transparent-fix.patch
+++ /dev/null
@@ -1,412 +0,0 @@
-diff --git a/android/app/src/main/java/com/admin/mamam/MainActivity.java b/android/app/src/main/java/com/admin/mamam/MainActivity.java
-index fb87e1e..203ad52 100644
---- a/android/app/src/main/java/com/admin/mamam/MainActivity.java
-+++ b/android/app/src/main/java/com/admin/mamam/MainActivity.java
-@@ -1,5 +1,21 @@
- package com.admin.mamam;
- 
-+import android.os.Bundle;
-+import androidx.core.view.WindowCompat;
- import com.getcapacitor.BridgeActivity;
- 
--public class MainActivity extends BridgeActivity {}
-+public class MainActivity extends BridgeActivity {
-+    // Edge-to-edge modern (API WindowCompat, bukan windowTranslucentStatus
-+    // yang deprecated & gak reliable lagi di API 35+ / Android 15).
-+    // setDecorFitsSystemWindows(false) yang bikin window app digambar di
-+    // BELAKANG status bar & navigation bar secara proper — ini yang bikin
-+    // Capacitor StatusBar.setOverlaysWebView(true) beneran ngefek transparan
-+    // tanpa bikin ikon jam/baterai jadi belang/samar seperti percobaan lama.
-+    // Kontrol warna ikon (terang/gelap) tetap dipegang JS lewat
-+    // StatusBar.setStyle() di App.jsx setiap kali tema light/dark berubah.
-+    @Override
-+    public void onCreate(Bundle savedInstanceState) {
-+        WindowCompat.setDecorFitsSystemWindows(getWindow(), false);
-+        super.onCreate(savedInstanceState);
-+    }
-+}
-diff --git a/android/app/src/main/res/values/styles.xml b/android/app/src/main/res/values/styles.xml
-index 65a98f4..ccd12db 100644
---- a/android/app/src/main/res/values/styles.xml
-+++ b/android/app/src/main/res/values/styles.xml
-@@ -7,16 +7,19 @@
-         <item name="colorPrimary">@color/colorPrimary</item>
-         <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
-         <item name="colorAccent">@color/colorAccent</item>
--        <!-- Edge-to-edge: biarin konten digambar di belakang status bar &
--             navigation bar. Ini yang bikin StatusBar.setOverlaysWebView(true)
--             dari Capacitor plugin beneran ngefek — kalau cuma diset di JS
--             tanpa ini, webview tetep "didorong turun" oleh sistem window
--             insets bawaan Android, jadi kelihatan sama aja kayak sebelumnya.
--             statusBarColor sengaja dibiarkan transparent di sini — warna
--             SOLID yang beneran keliatan diatur lewat StatusBar plugin di JS
--             (App.jsx), yang jalan tiap kali app dibuka/tema berubah. -->
--        <item name="android:windowTranslucentStatus">true</item>
-+        <!-- Edge-to-edge: konten digambar di belakang status bar & navigation
-+             bar. windowTranslucentStatus (cara lama) SENGAJA tidak dipakai —
-+             API itu deprecated & gak reliable lagi di targetSdk 35+ (Android
-+             15 mewajibkan edge-to-edge), penyebab ikon jam/baterai jadi
-+             belang/samar di percobaan sebelumnya. Setup edge-to-edge yang
-+             beneran ngefek sekarang ada di MainActivity.java lewat
-+             WindowCompat.setDecorFitsSystemWindows(false) (API modern).
-+             statusBarColor tetap transparent sebagai base — warna SOLID yang
-+             keliatan (mengikuti tema light/dark) diatur lewat Capacitor
-+             StatusBar plugin di JS (App.jsx), jalan tiap app dibuka/tema
-+             berubah. -->
-         <item name="android:statusBarColor">@android:color/transparent</item>
-+        <item name="android:navigationBarColor">@android:color/transparent</item>
-         <!-- android:windowLayoutInDisplayCutoutMode butuh API 28+ tapi aman
-              dideklarasikan di sini (minSdk 24) — cuma no-op di API lama. -->
-         <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="28">shortEdges</item>
-diff --git a/capacitor.config.json b/capacitor.config.json
-index 4fbc6a6..2f9144d 100644
---- a/capacitor.config.json
-+++ b/capacitor.config.json
-@@ -10,7 +10,7 @@
-     "StatusBar": {
-       "overlaysWebView": true,
-       "style": "DARK",
--      "backgroundColor": "#ffffff"
-+      "backgroundColor": "#00000000"
-     },
-     "SplashScreen": {
-       "launchShowDuration": 2000,
-diff --git a/package-lock.json b/package-lock.json
-index 3925ea9..5b0e592 100644
---- a/package-lock.json
-+++ b/package-lock.json
-@@ -3096,9 +3096,6 @@
-       "cpu": [
-         "arm64"
-       ],
--      "libc": [
--        "glibc"
--      ],
-       "license": "MIT",
-       "optional": true,
-       "os": [
-@@ -3115,9 +3112,6 @@
-       "cpu": [
-         "arm64"
-       ],
--      "libc": [
--        "musl"
--      ],
-       "license": "MIT",
-       "optional": true,
-       "os": [
-@@ -3134,9 +3128,6 @@
-       "cpu": [
-         "ppc64"
-       ],
--      "libc": [
--        "glibc"
--      ],
-       "license": "MIT",
-       "optional": true,
-       "os": [
-@@ -3153,9 +3144,6 @@
-       "cpu": [
-         "s390x"
-       ],
--      "libc": [
--        "glibc"
--      ],
-       "license": "MIT",
-       "optional": true,
-       "os": [
-@@ -3172,9 +3160,6 @@
-       "cpu": [
-         "x64"
-       ],
--      "libc": [
--        "glibc"
--      ],
-       "license": "MIT",
-       "optional": true,
-       "os": [
-@@ -3191,9 +3176,6 @@
-       "cpu": [
-         "x64"
-       ],
--      "libc": [
--        "musl"
--      ],
-       "license": "MIT",
-       "optional": true,
-       "os": [
-@@ -3487,9 +3469,6 @@
-         "arm"
-       ],
-       "dev": true,
--      "libc": [
--        "glibc"
--      ],
-       "license": "MIT",
-       "optional": true,
-       "os": [
-@@ -3504,9 +3483,6 @@
-         "arm"
-       ],
-       "dev": true,
--      "libc": [
--        "musl"
--      ],
-       "license": "MIT",
-       "optional": true,
-       "os": [
-@@ -3521,9 +3497,6 @@
-         "arm64"
-       ],
-       "dev": true,
--      "libc": [
--        "glibc"
--      ],
-       "license": "MIT",
-       "optional": true,
-       "os": [
-@@ -3538,9 +3511,6 @@
-         "arm64"
-       ],
-       "dev": true,
--      "libc": [
--        "musl"
--      ],
-       "license": "MIT",
-       "optional": true,
-       "os": [
-@@ -3555,9 +3525,6 @@
-         "loong64"
-       ],
-       "dev": true,
--      "libc": [
--        "glibc"
--      ],
-       "license": "MIT",
-       "optional": true,
-       "os": [
-@@ -3572,9 +3539,6 @@
-         "loong64"
-       ],
-       "dev": true,
--      "libc": [
--        "musl"
--      ],
-       "license": "MIT",
-       "optional": true,
-       "os": [
-@@ -3589,9 +3553,6 @@
-         "ppc64"
-       ],
-       "dev": true,
--      "libc": [
--        "glibc"
--      ],
-       "license": "MIT",
-       "optional": true,
-       "os": [
-@@ -3606,9 +3567,6 @@
-         "ppc64"
-       ],
-       "dev": true,
--      "libc": [
--        "musl"
--      ],
-       "license": "MIT",
-       "optional": true,
-       "os": [
-@@ -3623,9 +3581,6 @@
-         "riscv64"
-       ],
-       "dev": true,
--      "libc": [
--        "glibc"
--      ],
-       "license": "MIT",
-       "optional": true,
-       "os": [
-@@ -3640,9 +3595,6 @@
-         "riscv64"
-       ],
-       "dev": true,
--      "libc": [
--        "musl"
--      ],
-       "license": "MIT",
-       "optional": true,
-       "os": [
-@@ -3657,9 +3609,6 @@
-         "s390x"
-       ],
-       "dev": true,
--      "libc": [
--        "glibc"
--      ],
-       "license": "MIT",
-       "optional": true,
-       "os": [
-@@ -3674,9 +3623,6 @@
-         "x64"
-       ],
-       "dev": true,
--      "libc": [
--        "glibc"
--      ],
-       "license": "MIT",
-       "optional": true,
-       "os": [
-@@ -3691,9 +3637,6 @@
-         "x64"
-       ],
-       "dev": true,
--      "libc": [
--        "musl"
--      ],
-       "license": "MIT",
-       "optional": true,
-       "os": [
-@@ -4002,9 +3945,6 @@
-       "cpu": [
-         "arm64"
-       ],
--      "libc": [
--        "glibc"
--      ],
-       "license": "MIT",
-       "optional": true,
-       "os": [
-@@ -4021,9 +3961,6 @@
-       "cpu": [
-         "arm64"
-       ],
--      "libc": [
--        "musl"
--      ],
-       "license": "MIT",
-       "optional": true,
-       "os": [
-@@ -4040,9 +3977,6 @@
-       "cpu": [
-         "x64"
-       ],
--      "libc": [
--        "glibc"
--      ],
-       "license": "MIT",
-       "optional": true,
-       "os": [
-@@ -4059,9 +3993,6 @@
-       "cpu": [
-         "x64"
-       ],
--      "libc": [
--        "musl"
--      ],
-       "license": "MIT",
-       "optional": true,
-       "os": [
-@@ -8647,9 +8578,6 @@
-       "cpu": [
-         "arm64"
-       ],
--      "libc": [
--        "glibc"
--      ],
-       "license": "MPL-2.0",
-       "optional": true,
-       "os": [
-@@ -8670,9 +8598,6 @@
-       "cpu": [
-         "arm64"
-       ],
--      "libc": [
--        "musl"
--      ],
-       "license": "MPL-2.0",
-       "optional": true,
-       "os": [
-@@ -8693,9 +8618,6 @@
-       "cpu": [
-         "x64"
-       ],
--      "libc": [
--        "glibc"
--      ],
-       "license": "MPL-2.0",
-       "optional": true,
-       "os": [
-@@ -8716,9 +8638,6 @@
-       "cpu": [
-         "x64"
-       ],
--      "libc": [
--        "musl"
--      ],
-       "license": "MPL-2.0",
-       "optional": true,
-       "os": [
-diff --git a/src/app/App.jsx b/src/app/App.jsx
-index 83be432..abc10e0 100644
---- a/src/app/App.jsx
-+++ b/src/app/App.jsx
-@@ -217,25 +217,29 @@ export default function App() {
-   const [theme, setTheme, l23] = usePersistState('theme', 'light');
-   const [colorTheme, setColorThemeState] = usePersistState('colorTheme', 'orange');
- 
--  // ─── Status bar warna solid ─────────────────────────────────────────────
--  // Pendekatan solid (bukan overlay transparan): status bar dikasih warna
--  // background SAMA PERSIS dengan warna Header (#ffffff light / #020617
--  // dark = Tailwind slate-950). overlaysWebView di-set false supaya webview
--  // tetap didorong ke bawah status bar seperti biasa — jadi gak perlu
--  // urusan header transparan/blur "nembus" ke background window Android
--  // yang beda warna (itu penyebab status bar keliatan belang & ikon
--  // jam/baterai samar di percobaan overlay transparan sebelumnya).
-+  // ─── Status bar transparan edge-to-edge ─────────────────────────────────
-+  // overlaysWebView(true): webview digambar DI BELAKANG status bar (bukan
-+  // didorong ke bawahnya) — status bar jadi transparan, otomatis nyatu
-+  // dengan apa pun yang ada di baliknya (Header bg-white/95 light,
-+  // bg-slate-950/95 dark). Ini beda dari percobaan lama yang gagal: dulu
-+  // edge-to-edge diaktifkan lewat windowTranslucentStatus di styles.xml
-+  // (API lama, deprecated & gak reliable di targetSdk 35+/Android 15),
-+  // yang bikin ikon jam/baterai jadi belang/samar. Sekarang edge-to-edge
-+  // beneran diaktifkan lewat WindowCompat.setDecorFitsSystemWindows(false)
-+  // di MainActivity.java (API modern) — jauh lebih stabil.
-+  // Konten aman dari ketiban status bar karena AppLayout & Sidebar kasih
-+  // padding-top sebesar env(safe-area-inset-top) (lihat AppLayout.jsx).
-+  // Style ikon tetap ikut `theme`: Style.Dark (ikon gelap) di atas Header
-+  // terang (light mode), Style.Light (ikon terang) di atas Header gelap
-+  // (dark mode) — supaya jam/baterai/notifikasi selalu kontras & kebaca.
-   // Cuma jalan di native (Android/iOS); di browser biasa plugin ini no-op
-   // tapi dibungkus try/catch + cek platform biar aman.
-   useEffect(() => {
-     if (!Capacitor.isNativePlatform()) return;
-     (async () => {
-       try {
--        await StatusBar.setOverlaysWebView({ overlay: false });
--        await StatusBar.setBackgroundColor({ color: theme === 'dark' ? '#020617' : '#ffffff' });
--        // Style.Dark = ikon jam/baterai gelap (dipakai saat background status
--        // bar terang / light mode). Style.Light = ikon terang (dipakai saat
--        // background status bar gelap / dark mode).
-+        await StatusBar.setOverlaysWebView({ overlay: true });
-+        await StatusBar.setBackgroundColor({ color: '#00000000' });
-         await StatusBar.setStyle({ style: theme === 'dark' ? Style.Light : Style.Dark });
-       } catch (err) {
-         console.warn('StatusBar setup gagal (mungkin platform tidak didukung):', err);
-diff --git a/src/app/AppLayout.jsx b/src/app/AppLayout.jsx
-index a327c48..9fbb589 100644
---- a/src/app/AppLayout.jsx
-+++ b/src/app/AppLayout.jsx
-@@ -110,6 +110,13 @@ export default function AppLayout({
-     <div
-       ref={containerRef}
-       className="flex h-screen bg-slate-50 dark:bg-slate-950 font-body text-slate-800 dark:text-slate-100 overflow-hidden w-full relative"
-+      // paddingTop = tinggi status bar (notch/punch-hole Android, dynamic
-+      // island/notch iOS). Dipasang di container terluar (bukan cuma di
-+      // Header) supaya Sidebar overlay & elemen fixed lain ikut turun dan
-+      // gak ketiban status bar transparan juga. env(safe-area-inset-top)
-+      // bernilai 0 di browser biasa, jadi aman dipakai terus tanpa perlu
-+      // cek platform native/web.
-+      style={{ paddingTop: 'env(safe-area-inset-top)' }}
-     >
-       {sidebar}
- 
