diff --git a/src/app/App.jsx b/src/app/App.jsx
index c4297fd..b1ac4df 100644
--- a/src/app/App.jsx
+++ b/src/app/App.jsx
@@ -628,6 +628,10 @@ export default function App() {
   const setDeliveryFee = usePosStore((state) => state.setDeliveryFee);
   const customDeliveryFee = usePosStore((state) => state.customDeliveryFee);
   const setCustomDeliveryFee = usePosStore((state) => state.setCustomDeliveryFee);
+  const deliveryCourierId = usePosStore((state) => state.deliveryCourierId);
+  const setDeliveryCourierId = usePosStore((state) => state.setDeliveryCourierId);
+  const deliveryPaidTo = usePosStore((state) => state.deliveryPaidTo);
+  const setDeliveryPaidTo = usePosStore((state) => state.setDeliveryPaidTo);
   const manualDiscount = usePosStore((state) => state.manualDiscount);
   const setManualDiscount = usePosStore((state) => state.setManualDiscount);
   const pointsToRedeem = usePosStore((state) => state.pointsToRedeem);
@@ -815,6 +819,8 @@ export default function App() {
     setOrderType('Takeaway');
     setDeliveryFee(0);
     setCustomDeliveryFee('');
+    setDeliveryCourierId('');
+    setDeliveryPaidTo('kasir');
     setIsCartOpen(false);
   };
 
@@ -914,6 +920,8 @@ export default function App() {
     selectedCustomerId, setSelectedCustomerId,
     orderType, setOrderType,
     deliveryFee, setDeliveryFee,
+    deliveryCourierId, setDeliveryCourierId,
+    deliveryPaidTo, setDeliveryPaidTo,
     customDeliveryFee, setCustomDeliveryFee,
 
     // Customer
diff --git a/src/features/finance/CashTransferView.jsx b/src/features/finance/CashTransferView.jsx
index c770635..a47cf3e 100644
--- a/src/features/finance/CashTransferView.jsx
+++ b/src/features/finance/CashTransferView.jsx
@@ -19,19 +19,21 @@ function parseLocalDate(dateStr) {
  * SETORAN KURIR
  * ═══════════════════════════════════════════════════════════════════
  * Halaman ini BUKAN pengeluaran/pemasukan bisnis — ini cuma perpindahan
- * kas internal: uang yang tadinya dipegang kurir (dari hasil COD delivery
- * dikurangi belanja yang dia bayar pakai cash itu) dipindahkan balik ke
- * kasir/toko. Makanya disimpan di ledger terpisah (`cashTransfers`),
- * bukan ditambahkan ke expenses/incomes — supaya gak dobel-hitung di
- * Laporan Laba/Rugi (lihat penjelasan lengkap di utils/cashHolders.js).
+ * kas internal: uang yang tadinya dipegang kurir (dari hasil order
+ * Delivery yang dibayar COD ke kurir, dikurangi belanja yang dia bayar
+ * pakai cash itu) dipindahkan balik ke kasir/toko. Makanya disimpan di
+ * ledger terpisah (`cashTransfers`), bukan ditambahkan ke expenses
+ * ataupun bikin income baru — supaya gak dobel-hitung di Laporan
+ * Laba/Rugi (uangnya kan sudah tercatat lewat penjualan itu sendiri;
+ * lihat penjelasan lengkap di utils/cashHolders.js).
  *
  * Saldo per kurir dihitung REAL-TIME dari:
- *   incomes (cash masuk ke kurir) - expenses (cash keluar dari kurir) - cashTransfers (sudah disetor)
+ *   salesHistory (order Delivery COD ke kurir) - expenses (cash keluar dari kurir) - cashTransfers (sudah disetor)
  */
 const CashTransferView = () => {
   const {
     cashTransfers, setCashTransfers,
-    expenses, incomes, employees,
+    expenses, salesHistory, employees,
     triggerAlert, triggerConfirm, formatRupiah, isAdminMode
   } = useAppContext();
 
@@ -44,11 +46,14 @@ const CashTransferView = () => {
   const [showTrash, setShowTrash] = useState(false);
 
   // Saldo semua kurir, dihitung dari data aktif (recycle bin di-exclude).
+  // Cash masuk kurir MURNI dari salesHistory (order Delivery yang dibayar
+  // COD langsung ke kurir) — bukan dari incomes, karena kurir gak pernah
+  // menerima "pemasukan lain"/modal. Lihat utils/cashHolders.js.
   const balances = useMemo(() => computeAllCourierBalances(couriers, {
     expenses: activeOnly(expenses),
-    incomes: activeOnly(incomes),
+    salesHistory: activeOnly(salesHistory),
     cashTransfers: activeOnly(cashTransfers),
-  }), [couriers, expenses, incomes, cashTransfers]);
+  }), [couriers, expenses, salesHistory, cashTransfers]);
 
   const selectedBalance = useMemo(
     () => balances.find(b => b.employeeId === employeeId)?.balance ?? 0,
diff --git a/src/features/finance/IncomeView.jsx b/src/features/finance/IncomeView.jsx
index 5f72a1d..40ebe05 100644
--- a/src/features/finance/IncomeView.jsx
+++ b/src/features/finance/IncomeView.jsx
@@ -8,8 +8,6 @@ import { applySort } from '../../utils/sortUtils';
 import { markDeleted, restoreItem, activeOnly, trashedOnly } from '../../utils/softDelete';
 import { pushTransactionDelete } from '../../storage/realtimeSync';
 import { useBulkSelect } from '../../hook/useBulkSelect';
-import { getActiveCouriers } from '../../features/hrd/utils/payrollLogic';
-import { CASH_HOLDER_KASIR, makeCourierCashHolder, getCashHolder, cashHolderLabel } from '../../utils/cashHolders';
 
 // Parse string "YYYY-MM-DD" dari <input type="date"> sebagai LOCAL midnight.
 // PENTING: jangan pakai `new Date("YYYY-MM-DD")` langsung — JS selalu
@@ -24,15 +22,11 @@ function parseLocalDate(dateStr) {
 }
 
 const IncomeView = () => {
-  const { incomes, setIncomes, incomeCategories, setIncomeCategories, triggerAlert, triggerConfirm, formatRupiah, currentShift, isAdminMode, employees } = useAppContext();
+  const { incomes, setIncomes, incomeCategories, setIncomeCategories, triggerAlert, triggerConfirm, formatRupiah, currentShift, isAdminMode } = useAppContext();
   const [amount, setAmount] = useState('');
   const [category, setCategory] = useState(incomeCategories[0] || 'Lainnya');
   const [note, setNote] = useState('');
   const [dateInput, setDateInput] = useState(toLocalDateString());
-  // 'kasir' = uang langsung masuk laci kasir. employeeId kurir = kurir yang
-  // terima cash ini langsung dari customer (misal COD), belum disetor.
-  const [cashHolderId, setCashHolderId] = useState('kasir');
-  const couriers = useMemo(() => getActiveCouriers(employees), [employees]);
   const [filterMode, setFilterMode] = useState('month'); // 'month' | 'range' | 'all'
   const [filterMonth, setFilterMonth] = useState(toLocalMonthString());
   const [filterStartDate, setFilterStartDate] = useState('');
@@ -75,8 +69,6 @@ const IncomeView = () => {
     if (!dateInput) return triggerAlert('Pilih tanggal pemasukan!');
 
     const incomeDate = parseLocalDate(dateInput);
-    const courier = cashHolderId !== 'kasir' ? couriers.find(c => c.id === cashHolderId) : null;
-    const cashHolder = courier ? makeCourierCashHolder(courier) : CASH_HOLDER_KASIR;
 
     if (editingId) {
       // === MODE EDIT (ADMIN) ===
@@ -87,21 +79,20 @@ const IncomeView = () => {
             amount: Number(amount),
             category,
             note,
-            date: incomeDate,
-            cashHolder
+            date: incomeDate
           };
         }
         return inc;
       });
       setIncomes(updatedIncomes);
       setEditingId(null);
-      setAmount(''); setNote(''); setCashHolderId('kasir');
+      setAmount(''); setNote('');
       triggerAlert('Pemasukan berhasil diperbarui!');
     } else {
       // === MODE BUAT BARU ===
-      const newInc = { id: `INC-${Date.now()}`, amount: Number(amount), category, note, date: incomeDate, cashHolder };
+      const newInc = { id: `INC-${Date.now()}`, amount: Number(amount), category, note, date: incomeDate };
       setIncomes([newInc, ...incomes]);
-      setAmount(''); setNote(''); setCashHolderId('kasir');
+      setAmount(''); setNote('');
       triggerAlert('Pemasukan berhasil dicatat!');
     }
   };
@@ -112,8 +103,6 @@ const IncomeView = () => {
     setCategory(inc.category);
     setNote(inc.note);
     setDateInput(toLocalDateString(inc.date));
-    const holder = getCashHolder(inc);
-    setCashHolderId(holder.type === 'kurir' ? holder.employeeId : 'kasir');
   };
 
   const handleDeleteIncome = (id) => {
@@ -144,7 +133,6 @@ const IncomeView = () => {
     setEditingId(null);
     setAmount('');
     setNote('');
-    setCashHolderId('kasir');
     setDateInput(toLocalDateString());
   };
 
@@ -255,24 +243,6 @@ const IncomeView = () => {
             placeholder="Contoh: Tambahan Kas"
           />
 
-          {couriers.length > 0 && (
-            <div>
-              <label className="block text-xs font-bold text-slate-500 dark:text-slate-400 mb-1.5">Diterima Siapa?</label>
-              <Select
-                value={cashHolderId}
-                onChange={e => setCashHolderId(e.target.value)}
-              >
-                <option value="kasir">Kasir / Toko</option>
-                {couriers.map(c => <option key={c.id} value={c.id}>{c.name} (Kurir)</option>)}
-              </Select>
-              {cashHolderId !== 'kasir' && (
-                <p className="text-[10px] text-emerald-600 dark:text-emerald-400 mt-1 italic">
-                  *Uang ini diterima kurir (misal COD), masih di tangan kurir sampai disetor.
-                </p>
-              )}
-            </div>
-          )}
-
           <Button
             onClick={handleAddIncome}
             size="full"
@@ -390,7 +360,7 @@ const IncomeView = () => {
                       />
                     )}
                     <div className="flex-1">
-                      <p className="font-bold text-sm text-slate-800 dark:text-slate-100 flex items-center gap-2 flex-wrap">{inc.category} <Badge variant="neutral">{new Date(inc.date).toLocaleDateString('id-ID')}</Badge>{getCashHolder(inc).type === 'kurir' && <Badge variant="warning">💰 {cashHolderLabel(inc)}</Badge>}</p>
+                      <p className="font-bold text-sm text-slate-800 dark:text-slate-100 flex items-center gap-2 flex-wrap">{inc.category} <Badge variant="neutral">{new Date(inc.date).toLocaleDateString('id-ID')}</Badge></p>
                       <p className="text-[11px] text-slate-500 dark:text-slate-400 mt-1">{inc.note || 'Tanpa catatan'}</p>
                     </div>
                   </div>
diff --git a/src/features/pos/CartDrawer.jsx b/src/features/pos/CartDrawer.jsx
index a770fc1..9dc8645 100644
--- a/src/features/pos/CartDrawer.jsx
+++ b/src/features/pos/CartDrawer.jsx
@@ -34,7 +34,8 @@ const CartDrawer = () => {
     menus, setCurrentView, savedBills, triggerConfirm, formatRupiah, activeCustomer, 
     customerName, setCustomerName, setSelectedCustomerId,
     customers, setCustomers, orderType, setOrderType, deliveryFee, setDeliveryFee, 
-    customDeliveryFee, setCustomDeliveryFee, updateCartQty, updateCartItemNote, 
+    customDeliveryFee, setCustomDeliveryFee, deliveryCourierId, setDeliveryCourierId,
+    deliveryPaidTo, setDeliveryPaidTo, employees, updateCartQty, updateCartItemNote, 
     voucherInputCode, setVoucherInputCode, vouchers, appliedVoucher, setAppliedVoucher, 
     getSubtotal, triggerAlert, pointsToRedeem, setPointsToRedeem, getPointDiscount, 
     manualDiscount, setManualDiscount, getManualDiscountAmount, storeSettings, getDiscount, 
@@ -48,6 +49,8 @@ const CartDrawer = () => {
   // Itu penyebab nama pelanggan nyangkut lagi abis checkout & pas load saved bill kosong/guest,
   // soalnya auto-restore-nya kepicu tiap kali cart.length transisi dari 0 ke >0.
 
+  const couriers = activeOnly(employees || []).filter(e => (e.role || 'kasir') === 'kurir');
+
   // Reset semua state transaksi sekaligus (dipanggil saat keranjang dikosongkan total)
   const resetTransactionState = () => {
     setCart([]);
@@ -56,6 +59,8 @@ const CartDrawer = () => {
     setOrderType('Takeaway'); // sesuaikan kalau default order type bukan ini
     setDeliveryFee(0);
     setCustomDeliveryFee('');
+    setDeliveryCourierId('');
+    setDeliveryPaidTo('kasir');
     setVoucherInputCode('');
     setAppliedVoucher(null);
     setPointsToRedeem(0);
@@ -211,19 +216,60 @@ const CartDrawer = () => {
               </div>
 
               {orderType === 'Delivery' && (
-                <div className="bg-white dark:bg-slate-900 p-4 rounded-2xl shadow-sm border border-accent-100 dark:border-accent-500/20 animate-in slide-in-from-top-3 duration-300">
-                  <label className="block text-xs font-bold text-accent-600 dark:text-accent-400 uppercase tracking-wider mb-3 flex items-center gap-1"><Truck className="w-3 h-3" /> Biaya Pengiriman</label>
-                  <div className="flex overflow-x-auto pb-2 gap-2 snap-x hide-scrollbar">
-                    {[0, 3000, 5000].map(fee => (
-                      <button key={fee} onClick={() => { setDeliveryFee(fee); setCustomDeliveryFee(''); }} className={`snap-center shrink-0 py-2 px-4 rounded-xl border font-bold text-sm transition-all whitespace-nowrap ${deliveryFee === fee && !customDeliveryFee ? 'bg-accent-600 dark:bg-accent-500 text-white border-accent-600 dark:border-accent-500 shadow-md' : 'bg-white dark:bg-slate-900 text-slate-600 dark:text-slate-300 border-slate-200 dark:border-slate-700 hover:bg-slate-50 dark:hover:bg-slate-950'}`}>
-                        {fee === 0 ? 'Gratis' : formatRupiah(fee)}
-                      </button>
-                    ))}
-                    <div className="snap-center shrink-0 flex items-center gap-2 border rounded-xl px-2 bg-white dark:bg-slate-900 border-slate-200 dark:border-slate-700 min-w-[140px]">
-                      <span className="text-slate-400 dark:text-slate-500 text-xs font-bold pl-2">Rp</span>
-                      <input type="number" placeholder="Custom" className="w-full py-2 bg-transparent outline-none text-sm font-bold text-slate-700 dark:text-slate-200" value={customDeliveryFee} onChange={(e) => { setCustomDeliveryFee(e.target.value); setDeliveryFee(Number(e.target.value) || 0); }} />
+                <div className="bg-white dark:bg-slate-900 p-4 rounded-2xl shadow-sm border border-accent-100 dark:border-accent-500/20 animate-in slide-in-from-top-3 duration-300 space-y-4">
+                  <div>
+                    <label className="block text-xs font-bold text-accent-600 dark:text-accent-400 uppercase tracking-wider mb-3 flex items-center gap-1"><Truck className="w-3 h-3" /> Biaya Pengiriman</label>
+                    <div className="flex overflow-x-auto pb-2 gap-2 snap-x hide-scrollbar">
+                      {[0, 3000, 5000].map(fee => (
+                        <button key={fee} onClick={() => { setDeliveryFee(fee); setCustomDeliveryFee(''); }} className={`snap-center shrink-0 py-2 px-4 rounded-xl border font-bold text-sm transition-all whitespace-nowrap ${deliveryFee === fee && !customDeliveryFee ? 'bg-accent-600 dark:bg-accent-500 text-white border-accent-600 dark:border-accent-500 shadow-md' : 'bg-white dark:bg-slate-900 text-slate-600 dark:text-slate-300 border-slate-200 dark:border-slate-700 hover:bg-slate-50 dark:hover:bg-slate-950'}`}>
+                          {fee === 0 ? 'Gratis' : formatRupiah(fee)}
+                        </button>
+                      ))}
+                      <div className="snap-center shrink-0 flex items-center gap-2 border rounded-xl px-2 bg-white dark:bg-slate-900 border-slate-200 dark:border-slate-700 min-w-[140px]">
+                        <span className="text-slate-400 dark:text-slate-500 text-xs font-bold pl-2">Rp</span>
+                        <input type="number" placeholder="Custom" className="w-full py-2 bg-transparent outline-none text-sm font-bold text-slate-700 dark:text-slate-200" value={customDeliveryFee} onChange={(e) => { setCustomDeliveryFee(e.target.value); setDeliveryFee(Number(e.target.value) || 0); }} />
+                      </div>
                     </div>
                   </div>
+
+                  {couriers.length > 0 && (
+                    <div>
+                      <label className="block text-xs font-bold text-accent-600 dark:text-accent-400 uppercase tracking-wider mb-2">Diantar Kurir</label>
+                      <select
+                        value={deliveryCourierId}
+                        onChange={(e) => setDeliveryCourierId(e.target.value)}
+                        className="w-full px-3 py-2.5 rounded-xl border border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-900 text-sm font-bold text-slate-700 dark:text-slate-200 outline-none focus:border-accent-500 dark:focus:border-accent-500 transition-colors"
+                      >
+                        <option value="">Pilih Kurir (opsional)</option>
+                        {couriers.map(c => <option key={c.id} value={c.id}>{c.name}</option>)}
+                      </select>
+
+                      {deliveryCourierId && (
+                        <div className="mt-3">
+                          <label className="block text-xs font-bold text-accent-600 dark:text-accent-400 uppercase tracking-wider mb-2">Pembayaran Diterima Siapa?</label>
+                          <div className="grid grid-cols-2 gap-2">
+                            <button
+                              onClick={() => setDeliveryPaidTo('kasir')}
+                              className={`py-2 px-3 text-xs rounded-xl font-bold border transition-all ${deliveryPaidTo === 'kasir' ? 'bg-accent-600 dark:bg-accent-500 text-white border-accent-600 dark:border-accent-500 shadow-sm' : 'bg-white dark:bg-slate-900 text-slate-500 dark:text-slate-400 border-slate-200 dark:border-slate-700 hover:bg-slate-50 dark:hover:bg-slate-950'}`}
+                            >
+                              Sudah Lunas di Kasir
+                            </button>
+                            <button
+                              onClick={() => setDeliveryPaidTo('kurir')}
+                              className={`py-2 px-3 text-xs rounded-xl font-bold border transition-all ${deliveryPaidTo === 'kurir' ? 'bg-accent-600 dark:bg-accent-500 text-white border-accent-600 dark:border-accent-500 shadow-sm' : 'bg-white dark:bg-slate-900 text-slate-500 dark:text-slate-400 border-slate-200 dark:border-slate-700 hover:bg-slate-50 dark:hover:bg-slate-950'}`}
+                            >
+                              COD ke Kurir
+                            </button>
+                          </div>
+                          {deliveryPaidTo === 'kurir' && (
+                            <p className="text-[10px] text-accent-600 dark:text-accent-400 mt-2 italic">
+                              *Uang gak masuk laci kasir, dicatat sebagai dipegang kurir ini. Cek saldo di tab Setoran Kurir.
+                            </p>
+                          )}
+                        </div>
+                      )}
+                    </div>
+                  )}
                 </div>
               )}
 
diff --git a/src/features/pos/PaymentModal.jsx b/src/features/pos/PaymentModal.jsx
index abe7980..2cb2cfd 100644
--- a/src/features/pos/PaymentModal.jsx
+++ b/src/features/pos/PaymentModal.jsx
@@ -2,6 +2,7 @@ import React from "react";
 import { usePosStore } from '../../store/usePosStore';
 import { useAppContext } from '../../context/AppContext';
 import { generateUUID } from '../../utils/formatters';
+import { makeCourierCashHolder } from '../../utils/cashHolders';
 import {
   SplitSquareHorizontal,
   X,
@@ -28,7 +29,8 @@ const PaymentModal = () => {
     customers, setCustomers, claimsHistory, setClaimsHistory, getPointDiscount,
     manualDiscount, setManualDiscount, getManualDiscountAmount, customerName,
     orderType, getSubtotal, getDiscount, getTaxAmount, getServiceChargeAmount,
-    deliveryFee, salesHistory, setSalesHistory, setCustomerName, setAppliedVoucher,
+    deliveryFee, deliveryCourierId, deliveryPaidTo, employees, salesHistory, setSalesHistory,
+    setCustomerName, setAppliedVoucher,
     setVoucherInputCode, setPointsToRedeem, setReceiptModal, storeSettings,
     triggerAlert, navigate, resetDraft
   } = useAppContext();
@@ -72,6 +74,13 @@ const PaymentModal = () => {
       setClaimsHistory([{ id: `cl-${generateUUID()}`, customerId: activeCustomer.id, customerName: activeCustomer.name, rewardName: `Potongan Belanja ${formatRupiah(getPointDiscount())}`, pointsUsed: pointsToRedeem, date: new Date() }, ...claimsHistory]);
     }
 
+    // COD ke kurir: uang gak masuk laci kasir, jadi ditandai cashHolder
+    // di record order-nya sendiri (bukan bikin record income terpisah —
+    // lihat penjelasan lengkap di utils/cashHolders.js). Snapshot nama
+    // kurir dibekukan sama seperti pola snapshot lain di codebase.
+    const isCourierCOD = orderType === 'Delivery' && deliveryPaidTo === 'kurir' && deliveryCourierId;
+    const courier = isCourierCOD ? (employees || []).find(e => e.id === deliveryCourierId) : null;
+
     const newOrder = {
       id: `ORD-${generateUUID().split('-')[0].toUpperCase()}`, date: new Date(), customerName: customerName || 'Tanpa Nama', customerId: activeCustomer?.id || null, orderType, items: [...cart],
       subtotal: getSubtotal(), discount: getDiscount(), pointDiscount: getPointDiscount(),
@@ -84,7 +93,9 @@ const PaymentModal = () => {
       ojolName: method === 'Ojol' ? ojolName : null,
       orderNumber: method === 'Ojol' ? orderNumber : null,
       splitDetails: isSplitMode ? splitPayments : [],
-      hppTotal: cart.reduce((sum, item) => sum + (item.hpp * item.qty), 0)
+      hppTotal: cart.reduce((sum, item) => sum + (item.hpp * item.qty), 0),
+      courierId: isCourierCOD ? deliveryCourierId : null,
+      cashHolder: courier ? makeCourierCashHolder(courier) : undefined,
     };
 
     // 2. PROSES PENAMBAHAN POIN BARU (BERLAKU UNTUK SINGLE & SPLIT)
diff --git a/src/features/pos/ReceiptModal.jsx b/src/features/pos/ReceiptModal.jsx
index c2fb698..8db144a 100644
--- a/src/features/pos/ReceiptModal.jsx
+++ b/src/features/pos/ReceiptModal.jsx
@@ -143,6 +143,13 @@ const ReceiptModal = () => {
                             <span>{data.orderType}</span>
                         </div>
 
+                        {data.cashHolder?.type === 'kurir' && (
+                            <div className="flex justify-between mb-1 font-bold">
+                                <span>COD via:</span>
+                                <span>{data.cashHolder.employeeName}</span>
+                            </div>
+                        )}
+
                         {data.customerName && (
                             <>
                                 <div className="flex justify-between mb-1">
diff --git a/src/store/usePosStore.js b/src/store/usePosStore.js
index 7af3384..2f90072 100644
--- a/src/store/usePosStore.js
+++ b/src/store/usePosStore.js
@@ -47,6 +47,13 @@ export const usePosStore = create(
       orderType: 'Takeaway',
       deliveryFee: 0,
       customDeliveryFee: '',
+      // Khusus orderType 'Delivery': siapa yang antar, dan apakah customer
+      // bayar cash langsung ke kurir (COD) atau sudah lunas di kasir.
+      // 'kasir' = uang masuk laci kasir seperti biasa (default, gak ada
+      // perubahan perilaku). 'kurir' = uang dipegang kurir, ditandai
+      // cashHolder di record order-nya — lihat utils/cashHolders.js
+      deliveryCourierId: '',
+      deliveryPaidTo: 'kasir',
       manualDiscount: { type: 'fixed', value: 0 },
       pointsToRedeem: 0,
 
@@ -124,6 +131,8 @@ export const usePosStore = create(
       setOrderType: (type) => set({ orderType: type }),
       setDeliveryFee: (fee) => set({ deliveryFee: fee }),
       setCustomDeliveryFee: (fee) => set({ customDeliveryFee: fee }),
+      setDeliveryCourierId: (id) => set({ deliveryCourierId: id }),
+      setDeliveryPaidTo: (paidTo) => set({ deliveryPaidTo: paidTo }),
       setManualDiscount: (discount) => set({ manualDiscount: discount }),
       setPointsToRedeem: (points) => set({ pointsToRedeem: points }),
 
@@ -138,6 +147,8 @@ export const usePosStore = create(
         orderType: 'Takeaway',
         deliveryFee: 0,
         customDeliveryFee: '',
+        deliveryCourierId: '',
+        deliveryPaidTo: 'kasir',
         manualDiscount: { type: 'fixed', value: 0 },
         pointsToRedeem: 0,
       }),
@@ -155,6 +166,8 @@ export const usePosStore = create(
         orderType: state.orderType,
         deliveryFee: state.deliveryFee,
         customDeliveryFee: state.customDeliveryFee,
+        deliveryCourierId: state.deliveryCourierId,
+        deliveryPaidTo: state.deliveryPaidTo,
         manualDiscount: state.manualDiscount,
         pointsToRedeem: state.pointsToRedeem,
       }),
diff --git a/src/utils/cashHolders.js b/src/utils/cashHolders.js
index 6cbe837..4bb4895 100644
--- a/src/utils/cashHolders.js
+++ b/src/utils/cashHolders.js
@@ -4,15 +4,22 @@
 //  KONSEP "PEMEGANG KAS" (cash holder)
 // ═══════════════════════════════════════════════════════════════════════
 // Realita di lapangan: uang cash toko gak selalu ada di tangan kasir.
-// Kurir juga pegang cash — dari hasil COD delivery, dan kadang langsung
-// dipakai belanja tanpa disetor dulu ke kasir.
+// Kurir juga pegang cash — TAPI cuma dari SATU sumber: pesanan Delivery
+// yang dibayar customer LANGSUNG ke kurir pas barang nyampe (COD),
+// bukan dari "pemasukan lain" atau modal apapun.
 //
-// Daripada bikin 2 "source of truth" terpisah (buku kasir vs buku kurir),
-// kita tetap pakai SATU ledger (expenses & incomes yang sudah ada), tapi
-// setiap record dikasih tambahan info: SIAPA yang pegang fisik uangnya
-// saat transaksi itu terjadi (`cashHolder`).
+// Jadi cash masuk ke kurir itu bukan record baru yang diketik manual,
+// melainkan MELEKAT pada transaksi penjualan itu sendiri (`salesHistory`)
+// — order dengan `orderType: 'Delivery'` yang `deliveryPaidTo: 'kurir'`.
+// Kasir tinggal pilih siapa kurirnya & cara bayarnya pas checkout di POS;
+// gak ada input manual "pemasukan kurir" di modul Income sama sekali.
 //
-// Struktur `cashHolder` yang ditempel di expense/income:
+// Kadang kurir juga langsung belanja pakai uang itu sebelum sempat
+// disetor ke kasir — itu dicatat di ExpenseView dengan `cashHolder` yang
+// sama, jadi tetap SATU ledger (bukan 2 source of truth).
+//
+// Struktur `cashHolder` yang ditempel di record expense ATAUPUN order
+// salesHistory:
 //   { type: 'kasir' }                                  -> uang toko/laci kasir (default, berlaku utk data lama)
 //   { type: 'kurir', employeeId: 'EMP-xxx', employeeName: 'Budi' }  -> uang di tangan kurir tsb
 //
@@ -23,10 +30,11 @@
 // Field ini OPSIONAL & backward-compatible: record lama tanpa `cashHolder`
 // otomatis dianggap `{ type: 'kasir' }` (lihat getCashHolder di bawah).
 //
-// Selain expense/income, ada `cashTransfers` — ledger TERPISAH khusus buat
-// mencatat "setoran" kurir -> kasir (transfer internal, bukan pengeluaran
-// ataupun pemasukan bisnis, jadi sengaja gak dicampur ke expenses/incomes
-// supaya gak dobel-hitung di laporan Laba/Rugi).
+// Selain expense/salesHistory, ada `cashTransfers` — ledger TERPISAH khusus
+// buat mencatat "setoran" kurir -> kasir (transfer internal, bukan
+// pengeluaran ataupun pemasukan bisnis baru, jadi sengaja gak dicampur ke
+// expenses/incomes supaya gak dobel-hitung di laporan Laba/Rugi — uangnya
+// kan sudah tercatat masuk lewat penjualan Delivery COD itu sendiri).
 
 export const CASH_HOLDER_KASIR = { type: 'kasir' };
 
@@ -34,7 +42,7 @@ export function makeCourierCashHolder(employee) {
   return { type: 'kurir', employeeId: employee.id, employeeName: employee.name };
 }
 
-/** Resolve cash holder efektif dari sebuah record expense/income (default: kasir, utk data lama). */
+/** Resolve cash holder efektif dari sebuah record expense/order (default: kasir, utk data lama). */
 export function getCashHolder(record) {
   return record?.cashHolder && record.cashHolder.type ? record.cashHolder : CASH_HOLDER_KASIR;
 }
@@ -54,17 +62,17 @@ export function cashHolderLabel(record) {
  * Hitung saldo cash yang sedang dipegang SATU kurir tertentu, di luar
  * kas kasir/toko:
  *
- *   saldo = uang masuk (income yg diterima kurir, misal COD delivery)
+ *   saldo = uang masuk (order Delivery COD yg dibayar customer ke kurir)
  *         - uang keluar (expense yg dibayar pakai cash kurir, misal belanja)
  *         - total sudah disetor ke kasir (cashTransfers)
  *
  * Semua parameter sudah harus di-filter `activeOnly` dulu oleh pemanggil
  * (biar util ini gak perlu tau soal recycle bin).
  */
-export function computeCourierBalance(employeeId, { expenses = [], incomes = [], cashTransfers = [] } = {}) {
-  const cashIn = incomes
-    .filter(inc => isCourierHolder(inc) && getCashHolder(inc).employeeId === employeeId)
-    .reduce((sum, inc) => sum + (inc.amount || 0), 0);
+export function computeCourierBalance(employeeId, { expenses = [], salesHistory = [], cashTransfers = [] } = {}) {
+  const cashIn = salesHistory
+    .filter(order => isCourierHolder(order) && getCashHolder(order).employeeId === employeeId)
+    .reduce((sum, order) => sum + (order.total || 0), 0);
 
   const cashOut = expenses
     .filter(exp => isCourierHolder(exp) && getCashHolder(exp).employeeId === employeeId)
@@ -83,10 +91,10 @@ export function computeCourierBalance(employeeId, { expenses = [], incomes = [],
  * kurir yang punya aktivitas kas (cashIn/cashOut/deposit != 0) ATAU
  * yang saat ini masih berstatus kurir aktif (dari daftar `couriers`).
  */
-export function computeAllCourierBalances(couriers, { expenses = [], incomes = [], cashTransfers = [] } = {}) {
+export function computeAllCourierBalances(couriers, { expenses = [], salesHistory = [], cashTransfers = [] } = {}) {
   return (couriers || []).map(emp => ({
     employeeId: emp.id,
     employeeName: emp.name,
-    balance: computeCourierBalance(emp.id, { expenses, incomes, cashTransfers }),
+    balance: computeCourierBalance(emp.id, { expenses, salesHistory, cashTransfers }),
   }));
 }
